{
  "name": "auto-apply-helper-extension",
  "version": "1.0.0",
  "description": "Chrome extension for extracting jobs and autofilling applications.",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.25.0",
    "typescript": "^6.0.3"
  }
}
